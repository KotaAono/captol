from os.path import join, dirname

ENV_FILE = join(dirname( __file__), '../cache/env.json')
ICON_FILE = join(dirname(__file__), '../icon/icon.ico')
AREA_FILE = join(dirname(__file__), "../cache/areas.json")
