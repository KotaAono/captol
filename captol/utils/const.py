from os.path import join, dirname, abspath

ENV_FILE = abspath(join(dirname( __file__), '..', 'cache', 'env.json'))
ICON_FILE = abspath(join(dirname(__file__), '..', 'icon', 'icon.ico'))
AREA_FILE = abspath(join(dirname(__file__), '..', 'cache', 'areas.json'))
